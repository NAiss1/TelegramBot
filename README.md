# TelegramBot – Reminder Mini App

Telegram bot + WebApp (Mini App) that lets users create reminders in a clean UI,
and then receive scheduled messages directly in Telegram.

- Bot: Python + python-telegram-bot
- Web UI: HTML + CSS + JS (Telegram WebApp)
- Environment variables: BOT_TOKEN, WEB_APP_URL (no secrets in repo)
